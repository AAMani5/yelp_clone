<% if @resturants.any? %>
  <% @resturants.each do |resturant| %>
  <h2><%= link_to resturant.name, resturant_path(resturant) %></h2> <p><%= link_to "Edit #{resturant.name}", edit_resturant_path(resturant) %><%= link_to "Delete #{resturant.name}", resturant_path(resturant), method: :delete %></p>
  <% end %>
<% else %>
  No resturants yet
<% end %>

<a href='/resturants/new'>Add a resturant</a>
